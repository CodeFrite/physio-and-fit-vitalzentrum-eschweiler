# macOS resource forks, and the ._-prefixed probe files the measurement
# workflow writes next to index.html
._*
.DS_Store

# generated by build/make-artifact.py — a 3 MB single-file build with every
# image inlined, rebuilt on demand and not worth versioning
dist/
